Функция findMax:
Принимает на вход вектор целых чисел (const vector<int>& numbers).
Возвращает целое число - максимальный элемент в векторе.
Если вектор пуст, выводит сообщение об ошибке и возвращает INT_MIN.
Использует простой линейный поиск.
